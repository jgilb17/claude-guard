# claude-guard

Canonical source of the Claude Code safety guard (a PreToolUse hook).
One file, guard.mjs. Cloud setup scripts fetch it at container start; the Mac
refreshes it with the one-line command in df-correspondence CLAUDE.md.
